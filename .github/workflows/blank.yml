name: CI/CD Pipeline - Blood Donation Management System

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Verify system build
        run: echo "🩸 Running build checks for Blood Donation Management System"

      - name: Run simulated tests
        run: echo "✅ All tests passed successfully"

      - name: Simulated deployment
        run: echo "🚀 Application deployed successfully!"
        
    
